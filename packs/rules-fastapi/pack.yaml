name: rules-fastapi
version: 0.3.0
kind: rules
summary: "FastAPI conventions: routing, validation, errors and the Lambda handler."

requires:
  aidlc: ">=1,<2"

applies_when:
  frameworks: [fastapi]

emits:
  - agents_md
  - cursor_rules
  - copilot_instructions
  - claude_agents
