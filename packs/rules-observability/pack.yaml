name: rules-observability
version: 0.1.0
kind: rules
summary: "Observability: structured logs, traces, metrics and alarms that name what broke."

requires:
  aidlc: ">=1,<2"

applies_when:
  frameworks: [fastapi, aws, express, fastify]

emits:
  - agents_md
  - cursor_rules
  - copilot_instructions
