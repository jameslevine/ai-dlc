name: core
version: 0.3.1
kind: mixed
summary: >-
  Universal working agreement and the plan/build/review lifecycle. Installed in
  every repository.

requires:
  aidlc: ">=1,<2"

applies_when:
  always: true

emits:
  - agents_md
  - skills
  - claude_agents
