name: core
version: 0.2.0
kind: mixed
summary: >-
  Universal working agreement and the plan/build/review lifecycle. Installed in
  every repository.

requires:
  aidlc: ">=0.1,<1"

applies_when:
  always: true

emits:
  - agents_md
  - skills
  - claude_agents
