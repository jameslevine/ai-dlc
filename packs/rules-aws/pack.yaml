name: rules-aws
version: 0.2.0
kind: rules
summary: "AWS conventions from the Well-Architected pillars: least privilege, data protection, reliability and cost."

requires:
  aidlc: ">=0.1,<1"

applies_when:
  frameworks: [aws, aws-cdk, sam, cloudformation, terraform]

emits:
  - agents_md
  - cursor_rules
  - copilot_instructions
  - claude_agents
